name: Apply Incarnate message layer

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: true

      - name: Apply message layer patch
        run: python3 tools/apply_message_layer.py

      - name: Commit message layer
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'Onelsey'
          git config user.email '103017546+Wandraker@users.noreply.github.com'
          git add src/main/java/dev/onelsey/incarnate/possession/PossessionManager.java src/main/java/dev/onelsey/incarnate/ability/AbilityRegistry.java
          git commit -m 'Localize possession lifecycle [skip ci]'
          git push
