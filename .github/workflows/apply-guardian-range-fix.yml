name: Apply Guardian range fix

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: true

      - name: Apply Guardian range fix
        run: python3 tools/apply_guardian_range_fix.py

      - name: Commit Guardian range fix
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'Onelsey'
          git config user.email '103017546+Wandraker@users.noreply.github.com'
          git add src/main/java/dev/onelsey/incarnate/ability/AbilityRegistry.java
          git commit -m 'Keep Guardian laser inside configured range [skip ci]'
          git push
